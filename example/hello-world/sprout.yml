organization: mycompany
name: hello-world
description: 
git-repo: github.com/yourrepo
docker-repo: 
maintainers: 
# - name: bob 
#   email: bob@test.com

network:
  grpc:
    host: 0.0.0.0
    port: 3000
  http:
    enabled: true
    port: 8080
  graphql:
    enabled: true
    port: 8082

services:
  - name: helloworld
    description: Hello world!.

ci:
  system: circleci
  build-image: circleci/golang:1.12
  build-command: go build .
  test-command: go test -v ./...