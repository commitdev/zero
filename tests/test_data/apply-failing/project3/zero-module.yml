name: project3
description: 'project3'
author: 'Commit'

commands:
  check: sh check.sh
template:
  strictMode: true
  delimiters:
    - "<%"
    - "%>"
  inputDir: '.'
  outputDir: 'test'

requiredCredentials:
  - aws
  - github

parameters:
  - field: baz
    label: baz
