version: 2.1
jobs:
  build:
    docker:
      - image: {{ .CI.BuildImage }}
    steps:
      - checkout
      - run: 
        name: Build
        command: |
          {{ .CI.BuildCommand }}

  test:
    docker:
    steps:
      - checkout
      - run: 
        name: Test
        command: |
          {{ .CI.BuildCommand }}


workflow:
  version: 2.1
  build_and_test:
    jobs:
      - build
      - test